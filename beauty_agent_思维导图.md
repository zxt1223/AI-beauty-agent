# AI 美妆导购智能体（beauty_agent）思维导图

## 1. 项目概述
- 产品定位：AI 美妆导购智能体（Agent）——RAG 基础推荐问答
- 业务线归属：跨境电商 / 美妆个护导购
- 核心目标：把「用户在 Amazon 上大海捞针挑粉底液」变成「跟一个懂美妆的导购聊几句，就拿到对的推荐」
- 运行形态：本地可运行 Web Demo（零依赖后端 + 双语单页前端，双击启动）
- 数据源：Amazon Beauty 公开数据集（McAuley Lab）→ 清洗后 **1090 款商品** + 差评证据 + 评测集
- 迭代方式：**锚点回归制**（每次改动必须逐数字复现全部关键指标，否则退回）

## 2. 用户与痛点
- 目标用户
  - 英文用户（Amazon 主场景）
  - 中文跨境用户（平台中文心智）
  - 非英语语种用户（法/阿/俄/西）
- 痛点1 选不准肤质匹配：油皮买到水光、干皮买到控油，核心诉求买错
- 痛点2 专业参数看不懂：遮瑕度/质地/妆效/色号都是专业词，普通用户不会翻译成需求
- 痛点3 怕踩雷：评分看着高，实际卡粉/脱妆/闷痘，评论几千条没人看得完
- 痛点4 色号难挑：不知道哪款适合自己肤色，选错色号 = 白买
- 痛点5 语言门槛：非英语用户看不懂英文商品页
- 痛点6 选择过多：同品类几百款，没有任何个性化

## 3. 七大核心能力
- 能力1 多轮导购对话
  - 业务问题：一句话说不清需求，用户需要「问→答→调整」来回聊
  - 落地：追问节点（信息不够先反问，绝不硬推）+ 多轮上下文保留 + 本地对比矩阵（「这三款有什么区别」前端直接拼表）
- 能力2 用户记忆（跨会话）
  - 业务问题：老用户重开界面，导购应该记得他/她的肤质和语言
  - 落地：`user_profiles.json` 匿名画像（语言/肤质/最近访问）+ 时间感知问候（24h 再见：「还是以混油肤质为您推荐吗？」）
- 能力3 硬避雷
  - 业务问题：评分高 ≠ 适合，差评里的共性缺陷才是真相
  - 落地：差评轴共识 ≥ **70%** → 硬规则，命中即排除/沉底（避雷轴：卡粉/脱妆/闷痘/刺激/油腻）
- 能力4 多语种
  - 业务问题：用户用什么语言，就用什么语言服务
  - 落地：中文 → 混合（规则+LLM）；英文 → 纯规则；法/阿/俄/西 → 翻译桥再走英文规则
- 能力5 色号诊断
  - 业务问题：色号是个人化强约束，答错等于废单
  - 落地：先出「色号结论」再软询问，绝不突然转推荐
- 能力6 诚实兜底
  - 业务问题：AI 最忌不懂装懂硬编商品
  - 落地：检索空/意图不明 → 诚实告知；绝不编评分/评论原话
- 能力7 反馈闭环（AI Native 进化）
  - 业务问题：产品不是一次性交付，要从真实使用里持续变好
  - 落地：前端 👍/👎 → `ui_feedback.jsonl` 落盘 → `feedback_to_eval.py` 转 MySQL 待审表 → **人工确认后回灌金标准**（半自动防污染：误点/刷子先进待审，不自动进评测）

## 4. 技术方法总览：用了哪些模型、什么方法、结构什么样（核心）

### 4.1 模型矩阵（用了什么模型、用在哪、为什么）

| 模型/组件 | 类型 | 用在哪 | 为什么这么用 |
|---|---|---|---|
| **DeepSeek deepseek-v4-flash** | LLM | ① 中文意图直抽 ② 规则盲区兜底 ③ 对比/求助/闲聊润色 ④ 多语种翻译桥 | 便宜快，只当「兜底 + 语言桥」，**绝不替规则做检索决策** |
| **bge-small-en-v1.5** | 向量模型 | 语义检索（消融实验验证，主链路未启用） | 消融证明：标签约束主导的任务上不占优，留作演进方向 |
| **bge-reranker** | 交叉编码器重排 | 精排重排（消融 E 通道验证，主链路未启用） | 分数区间极窄、对标签轴失焦，只适合语义分支下游 |
| **规则引擎（无模型）** | 确定性系统 | **主链路核心**：约束抽取/标签打分/排序/避雷/追问/兜底 | 可解释、零成本、锚点稳定、可审计 |

> **模型策略一句话**：核心决策交给确定性规则引擎，LLM 只在规则盲区当兜底、在跨语言当翻译桥，深度学习检索（向量/重排）用消融证明「不上更好」——**先规则、后模型、模型只补盲区**。

### 4.2 核心方法（什么方法）

- **RAG 基础推荐**：商品库检索 → 打分排序 → 结构化回复（对 Amazon 商品做「基础模型推荐」）
- **知识分层标签**：肤质/妆效/遮盖/质地/色号 五个知识轴 + **隐式意图推理**（用户说症状，反推需求：说「防水」→ 隐含防晒场景；说「消除油光」→ 隐含油皮+哑光）
- **tagfirst 排序法**：`(标签分, 热度, BM25)` 字典序——**标签分绝对主序**，热度同分内排序，BM25 文本兜底
  - 热度 = **评分 × 评论量加成**（一个信号打包「质量 + 口碑规模」，自带反冷门）
- **多路召回 + 动态路由**（MVP 2026-09-01）：字段/文本/热销/语义**四路召回 → 并集候选池** → 结构化约束（含隐式意图）走 tagfirst（字段路=硬约束通过全集，与全库字节级一致）、无约束走语义通道——对齐企业级「召回→排序」范式；**route_trace 全量落埋点**（各路由多少、走哪个通道，可观测）
- **70% 避雷共识法（样本下限）**：评论负面反馈转硬规则（该商品负面评论 **≥5 条** 才参与判定，且某缺陷轴提及数 ÷ 该商品负面评论数 ≥70% → 硬排除；1~4 条偶发差评不避雷，2026-09-02 P0-1 补样本下限防单条差评误杀）
- **混合路由法**：规则为权威、LLM 只当语言桥/兜底（中文走混合、英文走规则、其他语种翻译）
- **置信度分支法**：意图闸门（llm_gate.py）>85% 直出 / 60~85% 生成+人工复核徽章 / <60% 直接转人工
  - **口径诚实**：分值为规则预分类固定分 + LLM 自评，**未统计校准**，85/60 是工程定标阈值（demo 无真实线上分布可校准，2026-09-02 措辞微调）
- **商品追问（product_qa）**：推荐后问「这三款会不会卡粉/适合干皮吗」→ 绑定刚推荐商品真实数据（标签/评分/条数/差评主题）逐款答 + 评论区声音，LLM 挂有确定性兜底；已知肤质/「哪三款」绝不反问（2026-09-01 用户 badcase 修复，闸门中文上下文补 CJK 抽取）
- **检索兑现率验证**：LLM 兜底意图不是自报置信度，而是「映射到规范轴 + 库内商品数达标 + top-8 真兑现率 ≥0.5」才采信
- **消融实验方法**：12 通道 A/B 对照（结构化/语义/双路由/交叉编码器/加权融合/trust 接线 ×5），用数据证明现排序唯一最优
- **评测闭环**：锚点回归 + CONTRACT 硬断言 + 资损陷阱 + 二期 A/B + 全库避雷
- **Harness 管控层**：权限门 / 会话预算 / LLM 行为预算 / 医疗护栏 / 全链路埋点
  - **现状边界（2026-09-02 措辞微调）**：harness.py 是轻量壳（上述五件套）；「工具拦截 + 置信度分支」真身在 llm_gate，完整 Harness 是演进目标架构非现状全部能力
- **可观测 & 反馈飞轮**：`harness_trace.jsonl` 全链路埋点（输入→路由→约束→决策→推荐→耗时→护栏，一条流水账，**每条推荐为什么出来都能回查**）→ `dashboard.py` 生成运营话看板 → 用户 👍/👎 走反馈闭环回灌评测——「系统能自证、能进化」

### 4.3 系统结构（结构长什么样）

```
[前端层]  双语单页聊天 / 商品卡片 / 本地对比矩阵 / 色号 chips / 👍👎 反馈按钮
   │  userId + 多轮上下文
[接入层]  零依赖后端 web_server / 语种自动路由 / 画像读写走存储接口 store.py
   │
[驾驭层]  Harness：权限门 · 会话预算 · LLM行为预算 · 医疗护栏 · 全链路埋点→harness_trace.jsonl
   │
[决策层]  三决策节点（追问 / 改写重试 / 诚实兜底）+ 意图闸门 + 隐式意图推理
   │
[检索层]  多路召回（字段/文本/热销/语义）→ 路由分流 → 精排接口 → 70%避雷硬过滤
          结构化约束（含隐式意图）→ tagfirst 排序 / 无约束 → 语义通道（BM25+向量）
          精排接口=换排序模型只改配置一行（当前冷启动，行为模型预留位）
          route_trace 落埋点（各路由多少、走哪个通道）
   │
[数据层]  1090款商品库 / 评测集 / 候选池 / 差评证据 / 用户画像 / 缓存 / MySQL
          harness_trace.jsonl（全链路流水） / ui_feedback.jsonl（用户反馈）
          存储接口 KVBackend→JSON 现行 / Redis 预留（换存储不换业务代码）
   │
[LLM层]  DeepSeek（只在盲区调，key 只存 scripts/.env）
   │
[评测层]  eval_*.py：锚点 94.7% / CONTRACT 105/105 / 资损 8/8 回归门
          反馈闭环：👍👎→待审→人工确认→回灌金标准（半自动防污染）
          dashboard.py 读埋点 → 运营话看板 dashboard.html（可观测）
```

## 5. 关键设计决策（深挖点）

- **为什么核心排序不用深度学习重排？**
  - 消融实测：交叉编码器重排 52.6% < 规则排序 94.7%——本任务是**领域标签约束主导**，不是通用语义匹配主导；deep 模型分数区间极窄、容易被「标题恰好命中关键词」的商品带偏
- **为什么 LLM 不当全权大脑？**
  - 确定性规则可解释、零成本、可审计、锚点稳定；LLM 只当兜底 + 语言桥，检索决策永远在规则侧 → 评测锚点零漂移
- **为什么 trust（评论数信任分）不进排序？**
  - 五重消融实证：trust 从「全局加权 → 第二键 → 同分综合 → 第三键」每个插入点都掉 ≥1 个命中（18/19→15/14/13/17）；它本质是热度「评论量」维度的粗糙子集，正确归属是**避雷护栏**而不是排序
- **为什么中文走 LLM、英文走规则？**
  - 中文模糊表达（「有点干」「不要太贵」）规则难覆盖 → LLM 直抽质量高；英文规则 24ms 内确定性回答零成本
- **为什么热度用「评分 × 评论量」？**
  - 一个信号打包「质量 + 口碑规模」，自带反冷门，是离线数据下最接近「行为信号」的内容代理
- **为什么做 12 通道消融？**
  - 证明现方案是数据支撑的最优解，而不是拍脑袋——**讲得出「为什么不那么做」比「为什么这么做」更值钱**
- **为什么做全链路埋点 + 看板 + 反馈闭环？**
  - ① 可观测：每条推荐为什么出来都能回查（被追问「这条怎么出来的」能当场拉证据）；② 运营话看板：指标对运营友好，不是给自己看的堆字段；③ 反馈闭环：AI Native 产品的进化机制，而不是一次性交付

## 6. 效果度量（锚点回归制）

| 指标 | 值 | 说明 |
|---|---|---|
| 首答命中率 | **18/19 = 94.7%** | 干净命中 top-3，定标口径 |
| NDCG@5 | 0.553 | 排序质量（Phase-1 池内） |
| 避雷率 | 0.889 | 差评商品不进 top-5 |
| CONTRACT 硬断言 | **105/105** | 决策正确性（追问/兜底/诚实/证据） |
| 追问率 / 降级率 / 兜底率 | 29.2% / 0% / 8.3% | 交互鲁棒性 |
| 资损陷阱 | **8/8** | 拒绝向用户推销不该推的（架构级回归门） |
| 二期 A/B（hidden 9 题） | 规则+LLM 兜底组 7/9 命中 | LLM 兜底有真实增量，锚点零漂移 |
| 12 通道消融 | A(ts,heat,bm25) = 94.7% 唯一最优 | H3(第三键 trust) 89.5% 最接近但 q22 证伪 |

> 中文快路径实测：LLM 直抽一次 11.9s → 规则直抽 0.04s；多语种、反馈、资损全部回归通过。

## 7. 诚实声明（局限与下一步）
- 局限1 无真实行为数据：数据是 Amazon 静态快照（评分/评论数/标题/标签），**没有 CTR/转化率/GMV 埋点**——无法做真行为信号排序
- 局限2 离线评测口径：首答/NDCG 都是离线池内评测，不是线上用户真实体验
- 局限3 覆盖品类有限：只做美妆粉底等导购场景，未扩展全品类
- 下一步1 行为信号演进：冷启动用「内容代理」（评分×评论量）→ 有埋点后用真实 CTR/GMV → 全链路在线实验（**路线图已成型 docs/enterprise_evolution.md，数据补齐即 config.RANKER="behavior" 一行切换**）
- 下一步2 企业级检索链路（**MVP 已落地 2026-09-01**）：多路召回（字段/文本/热销/语义）→ 路由分流 → 精排接口（冷启动 tagfirst，行为模型双塔预留位）→ 后置避雷/预算硬过滤；锚点 94.7%/105/105/8/8 零漂移；行为模型精排待 CTR/GMV 埋点数据后启用（局限1 的演进位）
- 下一步3 工程化收口（**已落地 2026-09-01**）：**数据看板** dashboard.py→dashboard.html（检索耗时/检索方式/每轮怎么处理/用户语言/成本护栏/热推 Top-10，明亮风 SaaS 设计语言；用户反馈「运营看不懂内部字段」→ **全翻译成大白话 + 每块灰字解释**）+ **存储接口抽象** store.py（KVBackend→JSON/Redis 预留位，换存储不换业务代码，锁语义不变）+ **反馈闭环**（👍/👎→待审→回灌金标准，半自动防污染）
- 对外口径：如实讲「离线评测体系完整、线上行为数据是已知局限、演进路径已想清楚（行为精排路线图 + 存储可演进接口）」
- 现状三分类盘点（2026-09-01）
  - ✅ 真落地：检索路由 / 避雷护栏 / 评测闭环 / 全链路埋点+看板 / 反馈闭环机制 / 存储抽象
  - 🔧 接口就位待演进：行为精排（LambdaRank 双塔） / 语义通道 / Redis / 粗排精排两级
  - ❌ 明确没做：行为埋点（CTR/GMV） / 在线实验平台 / 模型训练管线 / 运营后台 / 规模 / 告警
  - 结论：**架构形状参考企业级分层（管线/护栏/可观测/评测在单机 demo 上齐备），落地能力 ≠ 企业级（无真行为数据）**——讲「形状 + 缺口 + 演进路径」三步走

## 8. 术语表
- 规则引擎（Rule Engine）→ 确定性打分排序系统，无模型
- LLM → 大语言模型，本项目 DeepSeek 当兜底/语言桥
- RAG → 检索增强生成：先从商品库检索，再组织回答
- BM25 → 关键词相关性打分（文本检索）
- 标签分 → 肤质/妆效/遮盖/质地/色号五轴的知识分层匹配分
- tagfirst → 标签分绝对主序的排序法（先标签、再热度、再文本）
- 隐式意图 → 用户说症状、反推需求（防水→防晒场景）
- NDCG@5 → 排序质量指标（前 5 名里正确答案排得多靠前）
- 锚点回归 → 每次改动必须复现的基准数字（94.7%/105/105 等）
- 避雷共识 → 该商品负面评论 ≥5 条 且 差评轴提及 ≥70% 负面评论 → 硬规则排除（2026-09-02 补样本下限，防单条差评误杀）
- 多路召回 → 字段/文本/热销/语义四路取并集再排序（企业级检索范式；小库字段路≈全库，海量数据承重）
- 精排器接口 → 换排序模型只改 config 一行的预留位（当前冷启动 tagfirst，行为模型双塔待埋点数据）
- 消融实验 → 逐通道拆掉/替换某个组件，用数据证明它该不该存在
- 资损陷阱 → 会误导用户造成经济损失的问题（如故意推荐禁忌组合）
- Harness 管控层 → 给 Agent 加权限/预算/护栏/埋点的管理壳
- 用户画像 → 跨会话记住用户语言/肤质（匿名）
- 全链路埋点 → harness_trace.jsonl 一条流水账，每条推荐为什么出来都能回查
- 数据看板 → dashboard.py 读埋点生成静态页，指标翻译成运营大白话
- 反馈闭环 → 👍/👎 用户反馈 → 人工确认 → 回灌金标准（半自动防污染）
- 存储接口（KVBackend）→ store.py 抽象读写层，JSON 现行 / Redis 预留，换存储不换业务代码
- 商品追问（product_qa）→ 对刚推荐商品的属性/风险追问（会不会卡粉/适合谁），绑定商品真实数据 + 评论区声音逐款答
